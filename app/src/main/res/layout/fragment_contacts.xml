<?xml version="1.0" encoding="utf-8"?>
<!--
  Ruta: app/src/main/res/layout/fragment_contacts.xml

  Se usa CoordinatorLayout como contenedor principal para permitir que el
  FloatingActionButton se posicione correctamente sobre otros elementos.
-->
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ContactsFragment">

    <!--
      El RecyclerView que muestra la lista de contactos.
      Ocupa todo el espacio disponible.
    -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/addButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        app:srcCompat="@android:drawable/ic_input_add"
        app:backgroundTint="@color/orange_500"
        app:shapeAppearanceOverlay="@style/FabCircleOverlay"
        android:contentDescription="@string/add_contact_button_description" />


</androidx.coordinatorlayout.widget.CoordinatorLayout>
